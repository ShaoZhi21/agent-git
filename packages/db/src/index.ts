// @agent-git/db — Drizzle schema, migrations, and the typed DB client.
//
// Placeholder. Read ../../docs/conventions/data-model.md BEFORE adding tables.
// Every tenant-scoped table MUST have org_id + RLS; PKs are UUIDv7;
// checkpoints/eval_runs/eval_cases/regressions are append-only; add audit_log.
//
// TODO(F1+): define orgs, users, identities, installations, repos, agents,
//            checkpoints, eval_runs, eval_cases, regressions, datasets, audit_log.
export {};
