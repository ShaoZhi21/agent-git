{
  "name": "@agent-git/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Identity boundary (AuthN) + the single authorize() layer (AuthZ).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
