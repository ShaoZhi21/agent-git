// @agent-git/contracts — versioned EXTERNAL contracts (frozen once published).
//
// Read ../../docs/conventions/api-and-versioning.md BEFORE changing anything here.
// These shapes are public promises: the agentgit-json eval schema, the GitHub
// Action inputs, public API payloads. Additive-only within a version; breaking
// change => a new version. Never repurpose a field.
//
// TODO(F4): define the agentgit-json v1 zod schema (spec §7 F4) + emit JSON Schema.
export {};
