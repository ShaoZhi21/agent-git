// @agent-git/contracts — versioned EXTERNAL contracts (frozen once published).
// Read ../../docs/conventions/api-and-versioning.md before changing anything here.
export { systemContract } from './system.contract';

// TODO(F4): agentgit-json v1 schema (spec §7 F4).
// TODO(F1+): agents, checkpoints, eval-runs contracts (same ts-rest pattern).
