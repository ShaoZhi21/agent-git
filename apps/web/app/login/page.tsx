// Login → route "/login". Placeholder.
// TODO(F1): "Sign in with GitHub" starts the OAuth flow handled by the API.
export default function LoginPage() {
  return (
    <main>
      <h1>Sign in</h1>
      <p>Connect with GitHub to get started.</p>
    </main>
  );
}
