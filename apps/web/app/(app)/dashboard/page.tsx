// Dashboard → route "/dashboard". Placeholder.
// TODO(F2): list the current org's detected agents (fetched via the API).
export default function DashboardPage() {
  return (
    <main>
      <h1>Agents</h1>
      <p>Connected agents will appear here.</p>
    </main>
  );
}
